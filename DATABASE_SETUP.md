# Инструкция по настройке базы данных

Этот гайд поможет вам настроить и заполнить базу данных начальными данными для Bazarlar Online.

## Проблема: "Товары не найдены" / "Not Found" / Пустые фильтры

Если вы видите такие ошибки, это означает, что база данных пустая и нужно загрузить начальные данные.

## Решение

### Шаг 1: Убедитесь, что PostgreSQL и Redis запущены

**Если используете Docker:**
```bash
docker-compose -f docker-compose.dev.yml up -d
```

**Проверьте, что контейнеры запущены:**
```bash
docker-compose -f docker-compose.dev.yml ps
```

Вы должны увидеть:
- `bazarlar_postgres_dev` - Running
- `bazarlar_redis_dev` - Running

### Шаг 2: Создайте файл .env (если еще не создан)

```bash
# В корневой папке проекта
cp .env.local .env
```

Убедитесь, что в файле `.env` указаны правильные настройки для localhost:
```env
DATABASE_URL=postgresql+asyncpg://bazarlar_user:bazarlar_pass@localhost:5432/bazarlar_claude
DB_HOST=localhost
DB_PORT=5432
DB_NAME=bazarlar_claude
DB_USER=bazarlar_user
DB_PASSWORD=bazarlar_pass

REDIS_URL=redis://localhost:6379/0
REDIS_HOST=localhost
REDIS_PORT=6379
```

### Шаг 3: Загрузите начальные данные

**Для Windows:**
```powershell
# Перейдите в папку backend
cd backend

# Активируйте виртуальное окружение
venv\Scripts\activate

# Запустите seed скрипт
python seed.py
```

**Для Linux/macOS:**
```bash
# Перейдите в папку backend
cd backend

# Активируйте виртуальное окружение
source venv/bin/activate

# Запустите seed скрипт
python seed.py
```

### Шаг 4: Проверьте результат

Вы должны увидеть вывод примерно такой:
```
============================================================
🌱 Starting database seed process...
============================================================

🔨 Creating database tables...
✅ Tables created successfully
🏙️  Seeding cities...
✅ Added 9 cities
🏪 Seeding markets...
✅ Added 10 markets
📦 Seeding categories...
✅ Added 10 level 1 categories
✅ Added 18 level 2 categories

============================================================
🎉 Seed completed successfully!
============================================================

📊 Summary:
  - Cities: 9
  - Markets: 10
  - Categories: Added (check logs above)

✨ Your database is ready to use!
```

### Шаг 5: Запустите backend

```bash
# В папке backend (с активированным venv)
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Вы должны увидеть:
```
🚀 Starting Bazarlar Online...
✅ Application started successfully
INFO:     Uvicorn running on http://0.0.0.0:8000
```

### Шаг 6: Проверьте API

Откройте в браузере:
- **API Документация:** http://localhost:8000/api/docs
- **Проверка здоровья:** http://localhost:8000/health
- **Список городов:** http://localhost:8000/api/v1/locations/cities

Если всё настроено правильно, вы увидите список городов в JSON формате.

### Шаг 7: Запустите frontend

В **отдельном терминале**:

```bash
cd frontend
npm start
```

Frontend откроется на http://localhost:3000

Теперь фильтры должны загружать данные из базы!

## Что добавляет seed скрипт?

### Города (9 штук):
- Бишкек
- Ош
- Джалал-Абад
- Каракол
- Токмок
- Кара-Балта
- Нарын
- Талас
- Баткен

### Рынки (10 штук):
- Дордой (Бишкек)
- Ош базар (Бишкек)
- Аламединский рынок (Бишкек)
- Орто-Сайский рынок (Бишкек)
- Кудайберген (Бишкек)
- Жайма базар (Ош)
- Кара-Суу (Ош)
- Центральный рынок (Джалал-Абад)
- И другие...

### Категории (28 штук):
**Уровень 1 (10 основных категорий):**
- Электроника
- Одежда и обувь
- Дом и сад
- Детские товары
- Продукты питания
- Спорт и отдых
- Красота и здоровье
- Автотовары
- Хобби и творчество
- Услуги

**Уровень 2 (18 подкатегорий):**
- Телефоны и аксессуары
- Компьютеры и ноутбуки
- Женская одежда
- Мужская одежда
- Мебель
- Овощи и фрукты
- И многие другие...

## Troubleshooting

### Ошибка: "connection refused"
**Причина:** PostgreSQL не запущен или недоступен.

**Решение:**
```bash
# Проверьте статус Docker контейнеров
docker-compose -f docker-compose.dev.yml ps

# Если не запущены, запустите
docker-compose -f docker-compose.dev.yml up -d
```

### Ошибка: "Database already contains data"
**Причина:** Вы уже запускали seed скрипт ранее.

**Решение:** Скрипт спросит, хотите ли вы продолжить. Введите `y` для продолжения или `N` для отмены.

### Ошибка: "No module named 'app'"
**Причина:** Скрипт запускается не из правильной директории или venv не активирован.

**Решение:**
```bash
# Убедитесь, что вы в папке backend
cd backend

# Активируйте venv
source venv/bin/activate  # Linux/macOS
venv\Scripts\activate      # Windows

# Запустите снова
python seed.py
```

### Фронтенд всё еще показывает "Товары не найдены"
**Причина:** Backend не запущен или frontend подключается к неправильному API URL.

**Решение:**
1. Проверьте, что backend запущен: http://localhost:8000/health
2. Проверьте файл `frontend/.env`:
   ```env
   REACT_APP_API_URL=http://localhost:8000
   REACT_APP_SOCKET_URL=http://localhost:8000
   ```
3. Перезапустите frontend после изменения .env файла

## Дополнительные команды

### Очистка базы данных (полное удаление):
```bash
# Остановить контейнеры и удалить данные
docker-compose -f docker-compose.dev.yml down -v

# Запустить заново
docker-compose -f docker-compose.dev.yml up -d

# Загрузить данные снова
cd backend
python seed.py
```

### Подключение к PostgreSQL для проверки:
```bash
# Используя Docker
docker exec -it bazarlar_postgres_dev psql -U bazarlar_user -d bazarlar_claude

# Локально (если установлен psql)
psql -h localhost -U bazarlar_user -d bazarlar_claude

# Проверьте таблицы:
\dt

# Проверьте данные:
SELECT * FROM cities;
SELECT * FROM markets;
SELECT * FROM categories;

# Выход:
\q
```

### Проверка логов backend:
```bash
# Backend выводит логи в терминал
# Если нужно посмотреть подробности подключения к БД,
# убедитесь, что ENVIRONMENT=development в .env
```

## Следующие шаги

После загрузки начальных данных вы можете:

1. **Создать пользователя:** Зарегистрируйтесь через frontend
2. **Добавить товары:** Создайте продавца и добавьте товары
3. **Протестировать фильтры:** Попробуйте фильтровать по городам, рынкам и категориям

Удачи! 🚀
