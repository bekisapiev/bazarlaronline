# Деплой Bazarlar Online

Документация по развертыванию платформы Bazarlar Online.

## Варианты развертывания

### 🐳 [Локальный запуск через Docker](./DOCKER_LOCAL.md)
Для разработки и тестирования на локальной машине.

**Подходит для:**
- Разработка и отладка
- Тестирование новых функций
- Локальное окружение

**Требования:**
- Docker и Docker Compose
- 4 GB RAM
- 10 GB свободного места

### 🚀 [Деплой на beget.com](./BEGET_DEPLOYMENT.md)
Для production развертывания на сервере beget.com.

**Подходит для:**
- Production окружение
- Публичный доступ
- Высокая доступность

**Требования:**
- SSH доступ к серверу beget.com (46.173.18.202)
- Docker установлен на сервере
- Домен настроен на IP сервера

## Быстрый старт

### Локально (Docker)
```bash
# Клонируйте репозиторий
git clone https://github.com/your-org/bazarlaronline.git
cd bazarlaronline

# Скопируйте .env файлы
cp .env.example .env
cp frontend/.env.example frontend/.env

# Запустите Docker Compose
docker-compose up -d

# Платформа доступна на http://localhost:3000
```

### Production (beget.com)
```bash
# Подключитесь к серверу
ssh bazarlar@46.173.18.202

# Следуйте инструкциям в BEGET_DEPLOYMENT.md
```

## Структура директории

```
deployment/
├── README.md                      # Этот файл
├── DOCKER_LOCAL.md               # Инструкция для локального Docker
├── BEGET_DEPLOYMENT.md           # Инструкция для деплоя на beget.com
├── nginx.conf                     # Nginx конфигурация для сервера
├── nginx.docker.conf              # Nginx конфигурация для Docker
├── bazarlar-api.service          # Systemd сервис для API
├── bazarlar-celery.service       # Systemd сервис для Celery
└── deploy.sh                      # Скрипт автоматического деплоя
```

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `docker-compose logs`
2. Убедитесь, что все порты свободны
3. Проверьте .env файлы
4. Откройте issue в GitHub
